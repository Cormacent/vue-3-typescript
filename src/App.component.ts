import { Options, Vue } from "vue-class-component";
import CNavbar from "./components/c-navbar/CNavbar.vue";
@Options({
    components: {
        CNavbar
    }
})
export default class App extends Vue {

}